name: build

on:
  pull_request:
    branches:
      - main
    paths:
      - '**/*.swift'
      - '.github/workflows/build.yml'

jobs:
  build:
    runs-on: windows-latest

    steps:
    # - uses: compnerd/gha-setup-swift@main
    #  with:
    #    branch: development
    #    tag: DEVELOPMENT-SNAPSHOT-2021-07-24-a
    
    - run: |
        # https://uceee1bf865d4ef205c5bc2e84f0.dl.dropboxusercontent.com/cd/0/get/BaH2CfQFQslGT8l7CLhfJbMO1hPS5kSWpT-M2YnoApnO2SWyaDspCJP7RrPX5j24CPgg-K2GjNoixqQj7LMSxOlLmsM-1m441GhH135b8lwCP-hmfQLNIpwdL20d9HKZHbrXkwbBFZt0fae9hVs4__YV/file?_download_id=60388281517063379675825986238182383354194012728977422629797734348&_notify_domain=www.dropbox.com&dl=1
        function Update-EnvironmentVariables {
          foreach ($level in "Machine", "User") {
            [Environment]::GetEnvironmentVariables($level).GetEnumerator() | % {
              # For Path variables, append the new values, if they're not already in there
              if ($_.Name -Match 'Path$') {
                $_.Value = ($((Get-Content "Env:$($_.Name)") + ";$($_.Value)") -Split ';' | Select -Unique) -Join ';'
              }
              $_
            } | Set-Content -Path { "Env:$($_.Name)" }
          }
        }
        Install-Binary -Url "https://uc1c8615ebab78e04e2b692a7f0b.dl.dropboxusercontent.com/cd/0/get/BaIvEwTxferMD7iNGq8HmtaJFcLDnfS8ZIJGbPMmfSjQf5KkPIde-nVikSsDmQdVK6Dl5wwfDbiUsCwufBfNoP6pb8nNAmfb9MjQqOWlGSceWMf3BDOP26AHKY8wAg-rHBRLVwb3jdg7QyqFS4PLq5YF/file?_download_id=352361280161344546456563564525455266895581289289710442473367777283&_notify_domain=www.dropbox.com&dl=1" -Name "installer.exe" -ArgumentList ("-q")
        Update-EnvironmentVariables
        # Reset Path and environment
        echo "$env:Path" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8
        Get-ChildItem Env: | % { echo "$($_.Name)=$($_.Value)" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append }

    - uses: actions/checkout@v2

    #- name: Start SSH session
    #  uses: luchihoratiu/debug-via-ssh@main
    #  with:
    #    NGROK_AUTH_TOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}
    #    SSH_PASS: ${{ secrets.SSH_PASS }}
    #  env:
    #    UserName: runneradmin

    #- name: Setup tmate session
    #  uses: mxschmitt/action-tmate@v3

    #- name: Resolve
    #  run: swift package -v resolve --very-verbose -j 1

    - name: Build
      run: swift build -v

    - name: Run tests
      run: swift test -v -Xswiftc -DENABLE_TESTING
